{% set page = 'brand' %}

{% extends 'shared/base.html.twig' %}

{% block title %}RTA Speedbuilders - Add a brand of furniture{% endblock %}

{% block payload %}
{% set config = {display_poster: true} %}

<section>
  <div class="row">
    <div class="col">
      <h2 class="page-h2">Add a new Furniture Brand </h2>
    </div>
  </div>

  <form method="post">
    <fieldset class="form-row">
      <div class="form-group mb-3">
        <legend>Name </legend>
        {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.name) }}
        </div>
      <div class="form-group mb-3">
        <legend>Website</legend>
        {{ form_widget(form.website, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.website) }}
        </div>
      <div class="form-group mb-3">
        <button type="submit" class="btn btn-success rtaswc-fast">&check; submit</button>
      </div>
    </fieldset>
  {{ form_end(form) }}
</section>

{% endblock %}
